import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        main: {
            primary: "#21BFAF",
            secondary: "#B0D9D5",
            tertiary: "#D96F66",
            white: "#F2F0EB",
            black: "#0D0D0D",
        },
    },
});

export default theme;